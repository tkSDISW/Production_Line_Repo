---
id: obs-0003-3273df1a50424079aedc700e7166ba22
type: observation
timestamp: 2026-09-22T10:58:11Z
author: 
tags: []
---

## Capella state machine review — round 2 (pulled Tony's commit f817daf9)

**Model:** Production_Line_Equipment_Model, `[MSM] TR88 PackML 7 state` diagram on "Master TR88 Seven State" component.

### Fixes confirmed
- **Missing Stop transition — FIXED.** `Execute` now has an outgoing `stop` transition to `Stopped`; `Stopped` is reachable.

### Fix NOT confirmed — still open
- **Duplicate Aborted → Idle transition — still present.** Two separate StateTransition objects both go from `Aborted` to `Idle`: one triggered by ChangeEvent `Clear` (uuid 24896ad2), the other by a separate ChangeEvent `clear` (uuid 2b2535e8, lowercase). This was reported in the first review pass and has not actually been resolved yet.

### Naming normalization applied (this session, via apply_model_patch, commit 3c384f2f)
Lowercased 5 event trigger names per user request (events lowercase, state names left alone):
- `Start` → `start`
- `Suspend` → `suspend`
- `Hold` → `hold`
- `Unsuspend` → `unsuspend`
- `Stop` → `stop`

Already lowercase, untouched: `abort`, `reset`, `unhold`, `state complete`.

**Deliberately skipped:** `Clear` was NOT renamed to lowercase, because doing so would make it collide (identical name) with the existing separate `clear` event — compounding the open duplicate-transition issue rather than fixing it. Recommend resolving the duplicate (delete one of the two Aborted→Idle transitions) before normalizing this last one.

**Also flagged, not yet decided:** `state complete` is the one multi-word event name; everything else is a single word. Worth a style decision (e.g. `statecomplete`) for full consistency, once the duplicate is resolved.

**Reference:** NOTE-0002 (PackML state model), earlier review findings from the first capella-fabric session.
